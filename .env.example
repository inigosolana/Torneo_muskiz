# Copia este archivo a `.env` (nunca subas `.env` a Git).
# Valores en: Supabase → Project Settings → API

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Solo build Docker / servidor (no VITE_): clave de Gemini para edge functions locales si aplica
GEMINI_API_KEY=
